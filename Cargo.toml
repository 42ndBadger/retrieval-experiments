[package]
name = "retrieval-experiments"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
consensus-retrieval = { path = "../consensus-retrieval" }

[build-dependencies]
cmake = "0.1"

[profile.release]
lto = true
codegen-units = 1
